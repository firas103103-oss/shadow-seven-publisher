# الظل السابع — PostgreSQL

# ═══ API Base URL ═══
# فارغ = same-origin (الطلبات نسبية، Nginx يوجّه /api/shadow7/ → 8002)
# للإنتاج: فارغ يعمل مع Nginx proxy
VITE_API_URL=

# ═══ AI (Ollama محلي) ═══
VITE_OLLAMA_BASE_URL=/ai
VITE_OLLAMA_MODEL=llama3.2:3b

# ═══ App ═══
NODE_ENV=production
PORT=3000
